<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-screen bg-black px-4 text-white">
        <div class="w-full flex justify-end">

            <a class="bg-red-500 rounded-md px-3 py-2 text-sm inline-block mb-5" href="/logout">Logout</a>     
        </div>
          <h3 class="text-3xl mb-3"><span class="font-light">hello</span> , <%=user.username%></h3>  
           <h5 class="mb-5">you can create a new post</h5>
        <form action="/post" method="POST" class="flex flex-col items-center">
            <textarea placeholder="What's on you mind?" class = "w-2/3 resize-none bg-transparent
            border-2
             border-white-800 rounded-md " name="content"></textarea>
            <input type="submit" class="px-2 w-40  py-2 bg-blue-500 rounded-md block mt-2" value="Post">
        </form>
        <h3 class="text-zinc-400">Your post.</h3>
        <div class="postcontainer mt-5">
            <% user.posts.reverse().forEach(function(post){ %>
                <div class="post mb-4 w-1/3 p-4 rounded md border-[1px] border-zinc-800">
                    <h4 class="text-blue-500 mb-3"><%= user.name %></h4>
                    <p class="text-sm tracking-tight"><%= post.content %></p>
                    <small class="mt-2`"><%= post.likes.length %></small>
                    <div class="btns flex  gap-4 mt-4">
                        <a class="text-blue-600"href="/like/<%= post._id %>">
                            <%= post.likes.indexOf(user._id) > -1 ? 'unlike' : 'like' %>
                        </a>
                        <a class="text-zinc-600"href="/edit/<%= post._id %>">edit</a>
                    
                    </div>
                </div>
            <% }) %>  
        </div>
           
    
</body>
</html>